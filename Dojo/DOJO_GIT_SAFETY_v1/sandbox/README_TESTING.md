# Temporary DOJO GIT SAFETY v1 Sandbox

This directory is a non-live test fixture only.

For native Windows testing:

```bat
set DOJO_GIT_TEST_MODE=1
set DOJO_GIT_TEST_ROOT=<absolute path to this sandbox\dojo>
dojo_git.bat
```

Do not point `DOJO_GIT_TEST_ROOT` at the live `D:\Gemini\dojo`.
